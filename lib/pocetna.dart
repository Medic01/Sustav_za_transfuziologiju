import 'package:flutter/material.dart';

class PocetnaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pocetna'),
      ),
      body: Center(
        child: Text('Ovo je pocetna stranica.'),
      ),
    );
  }
}
