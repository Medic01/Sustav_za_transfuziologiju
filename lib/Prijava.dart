import 'package:flutter/material.dart';

class PrijavaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Prijava'),
      ),
      body: Center(
        child: Text('Ovo je stranica za prijavu.'),
      ),
    );
  }
}
