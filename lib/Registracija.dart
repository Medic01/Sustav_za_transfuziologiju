import 'package:flutter/material.dart';

class RegistracijaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registracija'),
      ),
      body: Center(
        child: Text('Ovo je stranica za registraciju.'),
      ),
    );
  }
}
