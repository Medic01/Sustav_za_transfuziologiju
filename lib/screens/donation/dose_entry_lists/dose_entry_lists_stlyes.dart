import 'package:flutter/material.dart';

const titleStyle = TextStyle(fontWeight: FontWeight.bold, color: Colors.white);
final titleBackgroundColor = Colors.red[900];
const appBarPadding = EdgeInsets.symmetric(horizontal: 8.0, vertical: 8.0);
final dropdownBoxDecoration = BoxDecoration(
  borderRadius: BorderRadius.circular(10.0),
  border: Border.all(
    color: Colors.grey,
    width: 1.0,
  ),
);
const dropdownContentPadding = EdgeInsets.symmetric(horizontal: 16.0);
const dropDownTextFieldColor = TextStyle(color: Colors.red);
const cardElevation = 3.0;
const cardMargin = EdgeInsets.all(8.0);